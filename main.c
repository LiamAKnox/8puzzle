#include <stdio.h>
//#include "8puzzle.h"

//game suite: currently working on 8puzzle, considering adding other games as practice
//perhaps translate my minesweeper coded in java to consolidate my work
int main(void) {
    //struct board *board = board_init(10, 10);
    //print_board(board);
    //destroy_board(board);
    printf("hello Liam");
}